* 하나의 프로그램 = 하나의 프로젝트

* 자바에서의 파일 생성
 - New Class로 생성 -> xxx.java 파일 생성

* 모든 프로그램은 1개의 main 메소드가 있어야 하며,
  main 메소드는 반드시 1개만 존재해야 함

* 화면 (console)에 출력 명령
 - System.our.println("출력할 문자열");
 - System.our.print("출력할 문자열");

* 키보드로 입력받는 방법
 - 스캔 객체(Scanner) 생성
 - 스캔 객체로 입력
 - 숫자 입력 : scan.nextInt();
   문자열 입력 : scan.nextLine();

* 객체, 인스턴스 클래스(class)
 - 객체(Object) : 개념 (자동차?)
 - 인스턴스(instance) : 객체를 실체화 한 것 (생성된 상태) (소나타?)
 - 클래스(class) : 객체를 정의해 놓은 것 (인스턴스를 만드는 설계도)

* 단축키
 - psvm : public static void main();
 - sout : System.out.println();

* 변수 : 데이터를 저장하는 공간
 - 변수의 선언
  : 자료형 변수명;
 - 변수의 초기화
  : 자료형 변수명 = 초기값;
  : 자료형 변수명;
  : 변수명 = 초기값;

* 자료형(Data Type) : 기본형 8개와 참조형으로 구성
  1) 숫자형
    - 정수형 : byte(1), short(2), int(4), long(8)
    - 실수형 : float(4), double(8)
  2) 문자형 : char(2)
  3) 불(boolean)형(논리형) : boolean(1)
  +) 참조형 String (문자열형) - 크기 제한 없음

* 참조형이란?
 - 메모리 상에서 변수의 위치와 데이터의 위치가 다른 상태.
 - 통상적으로 변수는 호출스택 영역에 생성되고, 데이터는 heap 영역에 생성.
 - 이 때, 변수에는 데이터의 위치정보(주소)가 저장되어 이 위치정보로 실제 데이터를 참조.
 - 따라서 참조형이라고 함.

* 출력문의 3가지 형태
  1) println : 출력 후 다음 라인으로 커서 이동
  2) print :
  3) printf("형식지정", 변수들); <- 잘 안 씀

* 이스케이프 문자
 - \t : tab 키
 - \n : 줄바꿈 강제

* 서식지정자
 - '%'와 조합하여 사용하는 문자
 - 종류
   1) %d : 정수변수의 값 출력 시
   2) %f : 실수변수의 값 출력 시
   3) %c : 문자변수의 값 출력 시
   4) %s : 문자열변수의 값 출력 시

  참고) 컴퓨터에서의 단위
       1) bit - 0/1 한자리
       2) byte - 8 bit (data 최소단위)

  참고) 부동소수점 - 0.1 = 0.1000 -> float